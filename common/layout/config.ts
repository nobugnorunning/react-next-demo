// This is the default configuration for the layout.

import type { ProLayoutProps } from "@ant-design/pro-layout";

export const defaultLayoutConfig: ProLayoutProps = {
  title: "React Next",
  layout: "mix",
  fixedHeader: true,
  fixSiderbar: true,
  contentWidth: "Fluid",
}
